/** @type {import('tailwindcss').Config} */
//const colors = require('tailwindcss/colors')

module.exports = {
//  mode: 'jit',
  content: ['./build/*.html}'],
    theme: {
      extend: {
        colors: {
          'emerald': '#50C878'
        }
      }
    },
  plugins: [],
}